 "use client";
import {useEffect,useState} from "react";import Nav from "../../components/Nav";import {createClient} from "../../lib/supabase/client";
export default function Employees(){const[x,setX]=useState<any[]>([]);useEffect(()=>{createClient().from("profiles").select("*").order("full_name").then(({data})=>setX(data||[]))},[]);return <><Nav/><main className="content"><h1 className="title">Employees</h1><p className="muted">Team members available for assignment.</p><div className="tablewrap"><table className="table"><thead><tr><th>Name</th><th>Phone</th><th>Role</th><th>Department</th><th>Active</th></tr></thead><tbody>{x.map(p=><tr key={p.id}><td>{p.full_name}</td><td>{p.phone||"-"}</td><td>{p.role}</td><td>{p.department||"-"}</td><td>{p.active?"Yes":"No"}</td></tr>)}</tbody></table></div></main></>}